import {Theme} from "@material-ui/core/styles";

export default (theme: Theme): any => ({
  mainContainer: {
    marginBottom: 10
  },
  displayNameInfo: {
    marginBottom: 20,
    textAlign: 'center'
  },
  displayNameContainer: {
    width: '27rem'
  }
});
